"""src.domain — domain package groupings for the Odysseus backend (ODY-19 / P2.2).

Sub-packages:
  auth/       — authentication helpers, session actions, user-time utilities
  calendar/   — CalDAV sync and write-back
  chat/       — chat processing, helpers, handler, ChatGPT subscription
  cookbook/   — model lifecycle, capabilities, context, discovery
  documents/  — document processing, actions, personal docs
  email/      — email thread parser
  infra/      — cross-cutting infrastructure (config, constants, exceptions, …)
  llm/        — LLM core, endpoint resolution
  research/   — deep research handler and utilities
"""
